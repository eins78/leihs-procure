{
  "name": "leihs-procurement",
  "private": true,
  "dependencies": {
    "leihs-procurement-ui": "file:./client",
    "serve": "^10.0.0"
  },
  "scripts": {
    "build-client": "cd client && npm ci --production && npm run -s build",
    "start": "react-scripts start"
  }
}
